# Defaults in variables.tf match the current NetChallenge inventory.
# Override only values that differ in another environment.

# keypair_name    = "kltn-autoscaling"
# public_key_path = "~/.ssh/kltn_autoscaling.pub"
